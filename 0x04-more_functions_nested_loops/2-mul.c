/**
 * mul - return multiplication of two integers
 * @a: is a parameter used to pass integer to func mul
 * @b: the same as @a
 * Return: result of multiplication (int type)
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}

