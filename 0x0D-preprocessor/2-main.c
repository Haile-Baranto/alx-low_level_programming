#include "stdio.h"
/**
 * main - prints the name of the file followed by new line
 * Return: 0 if successful else 1
 * FILE: 2-main.c
 * Auth: Haile W Baranto
 * Descripiton - using std library is allowed
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
