#ifndef FILE
#define FILE
#define ABS(X) ((X) >= 0 ? (X) : (-X))
/**
 * FILE: 3-function_like_macro.h
 * ABS(X):function-like macro that computes the absolute value of a number x
 * Auth: Haile W Baranto
 */
#endif
